{
  "summary": "SÍ se puede tener el motion nativo de ECharts de forma determinista y frame-exacta, y está verificado a tres niveles. (1) El reloj de animación de zrender es 100% \"patchable\": la clase Animation usa `new Date().getTime()` como fuente de tiempo y un `requestAnimationFrame` que se captura al cargar el módulo; `zr.animation` es propiedad pública, `update()` es invocable a mano y existen `pause()/resume()` — o sea, el estado de la animación es función pura del tiempo virtual si controlas Date+rAF. (2) El precedente más fuerte: el propio equipo de Apache ECharts corre sus tests de regresión visual sobre un timeline virtual (test/runTest/runtime/timeline.js sobreescribe rAF, setTimeout y Date a 16ms fijos por frame) para sacar screenshots deterministas de la animación NATIVA — es exactamente nuestra necesidad. (3) Las herramientas genéricas existen: timeweb (BSD-3) con `timeweb.goTo(ms)` (solo hacia adelante), timesnap/timecut (estables aunque con releases viejos, 2022-2023), y Chrome trae tiempo virtual NATIVO vía CDP `Emulation.setVirtualTimePolicy` + BeginFrame (Replit construyó su motor de render de video de producción exactamente así en 2025). Lo que NO existe: nadie ha publicado una integración ECharts-animado-dentro-de-Remotion; la guía oficial de Remotion dice explícitamente \"Disable all animations by third party libraries. They will cause flickering\" y su creador rechazó el time-hacking dentro de Remotion (\"hacking the time is not the way forward\") porque Remotion renderiza frames en paralelo/fuera de orden. La animación nativa que ganaríamos es real: transiciones automáticas enter/update/leave al hacer setOption, stagger por dato vía callback de animationDelay, universalTransition (morph entre tipos de serie + split/combine con groupId, 5.2+), keyframes (5.3) y valueAnimation de labels (números que ruedan interpolados, justo nuestro odómetro). Conclusión: la ruta segura es un microservicio \"plate renderer\" fuera de Remotion (Chrome headless + reloj virtual + screenshots PNG con alfa a 30fps) que se compone dentro de Remotion; la ruta \"dentro de Remotion\" es mecánicamente viable pero no soportada.",
  "key_points": [
    "RELOJ MANUAL — VERIFICADO EN FUENTE: zrender/src/animation/Animation.ts usa `getTime() = new Date().getTime()` como reloj y expone `update(notTriggerFrameAndStageUpdate?)` público + `pause()/resume()`; el loop usa rAF pero el delta se calcula con Date en cada llamada → patchear Date + llamar `zr.animation.update()` + `zr.flush()` da estado frame-exacto (https://raw.githubusercontent.com/ecomfe/zrender/master/src/animation/Animation.ts). ZRender expone `animation` como propiedad pública y `flush()` fuerza repintado síncrono (https://raw.githubusercontent.com/ecomfe/zrender/master/src/zrender.ts)",
    "CAVEAT DE TIMING DEL PATCH: zrender captura window.requestAnimationFrame con .bind(window) AL CARGAR EL MÓDULO (https://raw.githubusercontent.com/ecomfe/zrender/master/src/animation/requestAnimationFrame.ts) → el patch de rAF debe instalarse ANTES de importar echarts (evaluateOnNewDocument de puppeteer o import-order en bundle); el patch de Date puede ser en runtime",
    "PRECEDENTE CANÓNICO: el propio Apache ECharts corre sus visual regression tests sobre timeline virtual — test/runTest/runtime/timeline.js sobreescribe requestAnimationFrame, setTimeout y Date con FIXED_FRAME_TIME=16ms por frame para screenshots deterministas de la animación nativa (https://raw.githubusercontent.com/apache/echarts/master/test/runTest/runtime/timeline.js); NO mockean performance.now (zrender no lo usa)",
    "QUÉ HACE EL MOTION NATIVO (defaults verificados en src/model/globalDefault.ts de echarts): animation:'auto', animationDuration:1000, animationDurationUpdate:500, animationEasing/EasingUpdate:'cubicInOut', stateAnimation 300ms cubicOut (https://raw.githubusercontent.com/apache/echarts/master/src/model/globalDefault.ts). Al hacer setOption, ECharts diffea datos por name y aplica solo transiciones enter/update/leave; stagger por dato vía callback animationDelay (https://echarts.apache.org/handbook/en/how-to/animation/transition/)",
    "UNIVERSAL TRANSITION (5.2+): `universalTransition:true` permite morph entre CUALQUIER tipo de serie (pie→bar, bar→scatter, sunburst→treemap) y split/combine de datos vía groupId/dataGroupId en cada setOption — el efecto 'caro' imposible de recrear a mano (https://echarts.apache.org/handbook/en/basics/release-note/5-2-0/). 5.3 añade animación por keyframes con duración/delay/easing/loop por keyframe (https://echarts.apache.org/handbook/en/basics/release-note/5-3-0/). ECharts 5 soporta interpolación animada del TEXTO de labels (valueAnimation = números que ruedan, nuestro odómetro) (https://apache.github.io/echarts-handbook/en/basics/release-note/v5-feature/)",
    "ECHARTS+REMOTION: NO existe integración publicada (ni npm remotion-echarts ni docs). La best-practice oficial de Remotion para charts dice textual: 'Disable all animations by third party libraries. They will cause flickering during rendering' y manda todo a useCurrentFrame() (regla charts.md del skill oficial de Remotion, copia visible en https://github.com/vercel-labs/json-render/blob/main/skills/remotion-best-practices/rules/charts.md). Jonny Burger (creador de Remotion) rechazó explícitamente el time-hacking: 'The idea to hijack... the browser seems very hacky. For me it's a no-go' (2022) y 'we believe that hacking the time is not the way forward' (2023) — issue #399 (https://github.com/remotion-dev/remotion/issues/399). Razón: Remotion renderiza frames en paralelo/fuera de orden",
    "CAPTURA COMO PLATE — VIVA Y PROBADA EN PRODUCCIÓN: timeweb (BSD-3, v0.3.2 mayo-2023) crea timeline virtual paginal (Date.now, performance.now, setTimeout, rAF, Web Animations API, CSS via mutation observers, video seek) con API `timeweb.goTo(ms)` — solo hacia adelante (https://github.com/tungs/timeweb). timesnap/timecut lo envuelven con puppeteer+ffmpeg e incluyen `--transparent-background` y `--canvas-capture-mode` → PNG sequence CON ALFA del canvas de ECharts (https://github.com/tungs/timesnap, https://github.com/tungs/timecut). Releases viejos (2022/2023) pero el mecanismo es simple y timeweb se puede inyectar con puppeteer moderno propio",
    "ALTERNATIVA NATIVA DE CHROME (sin libs): CDP `Emulation.setVirtualTimePolicy` (experimental) con policies advance/pause/pauseIfNetworkFetchesPending + budget + evento virtualTimeBudgetExpired = tiempo virtual del navegador entero (https://chromedevtools.github.io/devtools-protocol/tot/Emulation/). Replit construyó su motor de render de video de producción exactamente con este enfoque (patch de setTimeout/setInterval/rAF/Date/performance.now + BeginFrame determinista + warmup del compositor): https://replit.com/blog/browsers-dont-want-to-be-cameras — validación industrial 2025 de que el reloj virtual escala a producción",
    "LÍMITE DEL SSR OFICIAL: `renderToSVGString` (init con renderer:'svg', ssr:true) embebe la animación INICIAL como CSS dentro del SVG sin JS, pero NO soporta bar racing, animaciones de labels ni efectos de lines (https://apache.github.io/echarts-handbook/en/how-to/cross-platform/server/) — insuficiente como fuente única de motion para nosotros, aunque el truco Remotion de animation-play-state:paused + animation-delay negativo podría scrubearlo frame-exacto para escenas de ENTRADA simples (https://www.remotion.dev/docs/third-party)",
    "RESTRICCIÓN goTo-forward-only PARA RENDER CHUNKED: timeweb solo avanza; como Remotion/Lambda parte el video en chunks que empiezan en frame N, cada worker debe fast-forwardear el reloj virtual de 0→N (barato: en tiempo virtual los callbacks disparan instantáneamente, sin esperar wall-clock; es el mismo replay que hace Replit al 'fire all pending callbacks' por frame)"
  ],
  "verdict_for_us": "SÍ es automatizable y determinista, con arquitectura de dos piezas. RUTA RECOMENDADA (plate service, fuera de Remotion): un microservicio Node con puppeteer moderno que (1) inyecta timeweb (o usa CDP Emulation.setVirtualTimePolicy) ANTES de cargar echarts, (2) monta el chart con la animación NATIVA activada (universalTransition, animationDelay stagger, valueAnimation odómetro, easings del theme congelado), (3) hace loop frame=0..N con goTo(frame*1000/30) + screenshot `--transparent-background` → secuencia PNG con alfa 1080-wide, (4) Remotion compone la secuencia (<Sequence>+<Img> o pre-encodeada a ProRes 4444/VP9-alpha vía FFmpeg) bajo nuestra capa tipográfica hueso/esmeralda. Es frame-exacto por construcción (mismo mecanismo que usa el equipo de ECharts para sus screenshots de regresión y que Replit usa en producción); para identidad de pixel entre corridas fijar versión de Chrome y --disable-gpu/SwiftShader. La ruta 'dentro de Remotion' (patchear Date+rAF pre-import y tickear zr.animation.update()+zr.flush() por frame desde useCurrentFrame) es mecánicamente válida según el código fuente de zrender, pero va contra la doctrina explícita de Remotion (flickering con render paralelo; Burger: time-hacking = no-go) — solo como experimento, no como base del pipeline. QUÉ RESUELVE PARA LA BARRA: nos regala la MECÁNICA pro (morph entre tipos de gráfica, diffing enter/update/leave, stagger por dato, números rodantes) que hoy el agente recrea mal a mano; QUÉ NO RESUELVE: el look igloo/Terminal no viene de default (cubicInOut 1000ms es pro pero genérico) — hay que congelar UN theme ECharts (colores #070707/#E9E7E0/#2ECB4F, easings, duraciones lentas, delays) como 'master' aprobado por Manuel una vez, y de ahí el LLM solo parametriza datos, nunca el motion. Encaja exactamente con la doctrina de masters: el craft vive en el motor de ECharts + theme congelado, no en código autorado por el agente.",
  "sources": [
    "https://raw.githubusercontent.com/ecomfe/zrender/master/src/animation/Animation.ts",
    "https://raw.githubusercontent.com/ecomfe/zrender/master/src/zrender.ts",
    "https://raw.githubusercontent.com/ecomfe/zrender/master/src/animation/requestAnimationFrame.ts",
    "https://raw.githubusercontent.com/apache/echarts/master/test/runTest/runtime/timeline.js",
    "https://raw.githubusercontent.com/apache/echarts/master/src/model/globalDefault.ts",
    "https://echarts.apache.org/handbook/en/how-to/animation/transition/",
    "https://echarts.apache.org/handbook/en/basics/release-note/5-2-0/",
    "https://echarts.apache.org/handbook/en/basics/release-note/5-3-0/",
    "https://apache.github.io/echarts-handbook/en/basics/release-note/v5-feature/",
    "https://apache.github.io/echarts-handbook/en/how-to/cross-platform/server/",
    "https://github.com/tungs/timeweb",
    "https://github.com/tungs/timesnap",
    "https://github.com/tungs/timecut",
    "https://chromedevtools.github.io/devtools-protocol/tot/Emulation/",
    "https://replit.com/blog/browsers-dont-want-to-be-cameras",
    "https://github.com/remotion-dev/remotion/issues/399",
    "https://www.remotion.dev/docs/third-party",
    "https://github.com/vercel-labs/json-render/blob/main/skills/remotion-best-practices/rules/charts.md",
    "https://github.com/apache/echarts/pull/11154"
  ]
}