{
  "summary": "Los sistemas de motion profesionales resuelven exactamente nuestro problema con un patrón repetido en tres industrias distintas: (1) design systems de marca (Material, IBM Carbon, Uber Base, Slack) congelan el motion en TOKENS con nombre (2-6 curvas cubic-bezier + escala de duraciones en ms) que se distribuyen COMO CÓDIGO (paquete npm), de modo que nadie re-decide un bezier por pieza — solo se elige un token con reglas de uso documentadas; (2) los estudios trabajan por stage-gates con lock duro: brief → guion → storyboard → styleframes aprobados → animación → \"picture-lock\", y el spec de un master de motion (formato IBM) lista por elemento: transformación con valores exactos, token de duración, token de curva, delay y un choreography chart tipo Gantt; (3) el broadcast (Vizrt/Chyron, usado por CNBC y BBC) es literalmente nuestro caso: un diseñador autora la escena UNA vez con \"Transition Logic\" (estados nombrados + transiciones diseñadas entre ellos) y la compila a un template que solo expone CAMPOS DE DATOS; el periodista (en nuestro caso, el LLM) llena campos y físicamente NO PUEDE tocar el movimiento. Existe además un formato estándar para serializar esto: el W3C Design Tokens Format (primera versión estable oct-2025) tiene tipos `duration` y `cubicBezier` en JSON. Lottie/Rive sirven como \"spec ejecutable\" (la animación aprobada es la que se reproduce), pero para Remotion server-side el camino más directo es tokens JSON + componentes-master con props de datos únicamente. Remotion es el sustrato correcto porque su modelo es determinista por diseño (todo derivado de useCurrentFrame(); frame 42 siempre idéntico). Conclusión: el motion de clase mundial no sale del que autora cada video — sale de autorar UNA vez por familia (con iteración humana hasta pasar la barra), congelarlo como master+tokens, y degradar al LLM al rol de periodista de redacción: elegir template y llenar datos.",
  "key_points": [
    "TOKENS, NO DECISIONES: Material Design congela el motion en tokens de easing+duración con reglas de pareo; valores concretos verificados (M1): standard cubic-bezier(0.4,0,0.2,1), deceleración (0,0,0.2,1) para entradas, aceleración (0.4,0,1,1) para salidas, sharp (0.4,0,0.6,1); duraciones 300ms estándar móvil, 225ms entrada, 195ms salida, y regla dura '>400ms se siente lento' — https://m1.material.io/motion/duration-easing.html ; M3 lo evolucionó a esquemas (expressive/standard) × velocidades (fast/default/slow) × tipos (spatial/effects) — https://m3.material.io/styles/motion/easing-and-duration/tokens-specs",
    "TOKENS COMO CÓDIGO: IBM Carbon reduce TODO su motion a 6 curvas (standard/entrance/exit × productive/expressive) distribuidas como paquete npm @carbon/motion con funciones/mixins — el implementador importa motion('standard','productive') y nunca escribe un bezier a mano — https://carbondesignsystem.com/elements/motion/overview/ y https://www.npmjs.com/package/@carbon/motion",
    "FORMATO DEL SPEC DE UN MASTER (IBM, verificado): cada spec de motion contiene por elemento: (a) elemento+propiedad animada, (b) transformación con valores exactos ('move 300px left'), (c) duración (feedback <400ms), (d) curva como TOKEN nombrado (nunca curva custom), (e) delay, (f) choreography chart = timeline visual tipo Gantt de cuándo se mueve cada elemento; y se entrega prototipo animado + spec juntos — https://medium.com/design-ibm/working-together-ux-motion-specs-for-design-and-development-249b12d60909",
    "VOCABULARIO CERRADO, NO PARÁMETROS LIBRES: Uber Base define el motion como 'atomic animation patterns' (enter/exit, transformation) que se COMBINAN — la creatividad ocurre eligiendo del vocabulario, no inventando curvas; 'Base choreography defines how we move (and how we don't move)' — https://base.uber.com/6d2425e9f/p/116184-motion",
    "CONSTITUCIÓN + PROHIBICIONES (Slack): su 'animation constitution' incluye valores (Engaging/Purposeful/Clear/Delightful), técnicas PROHIBIDAS explícitas ('never animate character by character', sin motion blur en 2D), animaciones de logo pre-aprobadas y templates estándar de bumpers/transiciones — construida específicamente para que estudios externos/freelancers produzcan motion consistente sin re-decidir nada — https://tsmotion.design/slack-motion-design-guidelines (sistema hecho por Tatiana Shchekina; guía oficial en brand.slackhq.com/motion)",
    "PROCESO DE ESTUDIO CON LOCKS (verificado): flujo estándar = brief → guion → storyboard (aprobación) → STYLEFRAMES estáticos con el look final (aprobación) → animación con audio scratch (rondas) → PICTURE-LOCK → VO/música/SFX → export; el lock es un gate formal: nada de audio hasta que el movimiento está congelado — https://runner.studio/blog/this-is-the-workflow-every-motion-design-studio-uses/",
    "PASOS PARA SISTEMATIZAR MOTION (designsystems.com, Invision): (1) auditar motion existente, (2) principios, (3) building blocks = escala de timings tipo tipografía (t1-t5) + SOLO 2-3 curvas de easing + efectos con nombre ('Card-Fade-In') + patrones de coreografía, (4) specs de traducción (diagramas Gantt o texto con valores exactos de opacity/scale/posición) — https://www.designsystems.com/5-steps-for-including-motion-design-in-your-system/",
    "BROADCAST = NUESTRO CASO EXACTO — Vizrt Transition Logic: el diseñador construye una Master Scene con ESTADOS nombrados (LARGE/SMALL, OTS_LEFT/OTS_RIGHT) y anima las transiciones entre estados UNA vez; después 'permite al diseñador mantener el look and feel mientras cualquier otra persona (periodistas/productores) agrega gráficos al rundown sin conocimiento técnico' — el operador NO PUEDE romper el motion aunque quiera — https://docs.vizrt.com/viz-artist-guide/5.4/Transition_Logic.html",
    "GOBERNANZA DE TEMPLATES EN REDACCIONES: Viz Pilot Edge — diseñadores crean templates en Template Builder, periodistas solo llenan campos desde el browser; los templates se etiquetan por show/concepto/variante para que cada equipo solo VEA los que le tocan ('reducing clutter and risk of errors'); 'concepts and variants' = crear una vez, usar en múltiples shows — https://www.vizrt.com/products/viz-pilot-edge/ ; datos en vivo auto-llenan templates vía Viz Trio+Datacenter (finanzas/elecciones/deportes) — https://www.vizrt.com/products/viz-trio/",
    "CASOS REALES: CNBC lanzó su look completo en Vizrt — el equipo de diseño de 30 Rock + Reality Check Systems construyeron los templates iniciales de Pilot; charts, tickers, banners, bugs y walls salen todos del mismo sistema — https://tkt1957.com/cnbc-vizrt-success-story/ ; BBC News Studio B: todas las pantallas corren Viz con 'multi-form templates donde el periodista tiene todo lo que pueda necesitar en el template, que se renderiza como tower, catwalk o full screen' — https://www.tvbeurope.com/live-production/we-wanted-a-studio-that-was-physical-not-virtual-behind-the-design-and-technology-of-bbc-news-studio-b",
    "EL MISMO PATRÓN EN ADOBE: Motion Graphics Templates (.mogrt) — el motion designer 'retiene control estilístico de la composición' y SOLO las propiedades que él decide exponer son editables por el editor en Premiere; es el mecanismo de lock más accesible de la industria — https://helpx.adobe.com/after-effects/using/creating-motion-graphics-templates.html",
    "FORMATO ESTÁNDAR PARA SERIALIZAR MOTION: el W3C Design Tokens Format Module alcanzó su primera versión estable (28-oct-2025) e incluye tipos $type:'duration' y $type:'cubicBezier' (value=[P1x,P1y,P2x,P2y]) en JSON, con módulo de Motion como extensión planificada — https://www.designtokens.org/tr/drafts/format/ y https://www.w3.org/community/design-tokens/2025/10/28/design-tokens-specification-reaches-first-stable-version/",
    "SPEC EJECUTABLE: Lottie convierte la animación de AE en JSON y 'la animación que el diseñador aprobó es exactamente la que se embarca, renderizada idéntico por ThorVG' (usable en Remotion vía @remotion/lottie); Rive usa state machines visuales donde 'los inputs son el contrato entre diseñador y desarrollador' — https://lottiefiles.com/tutorials/how-to-hand-off-your-lottie-animation-to-developers-04oq0VrzTwo y https://rive.app/blog/how-state-machines-work-in-rive — CAVEAT: la página del producto 'Motion System' de LottieFiles devolvió 403, sus claims solo se verificaron vía snippets de búsqueda",
    "REMOTION ES EL SUSTRATO CORRECTO PARA MOTION CONGELADO: la doc oficial exige que las animaciones deriven puramente de useCurrentFrame() — 'piensa en tu componente como una función que transforma un número de frame en una imagen'; mismo frame = mismo pixel siempre, prohibido depender de orden de render o de aleatoriedad — determinismo frame-exacto nativo — https://www.remotion.dev/docs/flickering y https://www.remotion.dev/docs/use-current-frame",
    "NO VERIFICADO (honestidad): no existe documentación pública del sistema de motion tokens de Netflix (solo su identidad sonora Ta-dum está documentada) ni encontré un sistema público de motion de Spotify; la página M3 tokens-specs no se pudo fetchear directamente (app JS) — su contenido se verificó vía resúmenes de búsqueda; el hilo de TV Forum sobre software de BBC es fuente débil (foro)"
  ],
  "verdict_for_us": "Adoptar el modelo BROADCAST + DESIGN-SYSTEM, que es exactamente la separación que ya intuye la doctrina de masters: (1) TOKENS PRIMERO — un solo archivo motion.tokens.json en formato W3C DTCG con: 2-3 curvas cubic-bezier máximo (estilo igloo/Terminal: probablemente una expo-out lenta para entradas y una simétrica para morphs), una escala de duraciones tipo t1-t5 en FRAMES (no ms, para exactitud a 30fps), delays/stagger nombrados, y el destello gris→esmeralda→hueso como UNA coreografía nombrada con su timeline fijo; los componentes Remotion importan tokens, y un lint/CI gate rechaza cualquier literal de duración/bezier/spring en código de escena (equivalente automatizado de @carbon/motion). (2) MASTERS COMO TRANSITION LOGIC — cada familia de escena es un componente con estados nombrados y transiciones diseñadas UNA vez entre ellos; sus props son EXCLUSIVAMENTE datos (textos, cifras, series, duración de VO), nunca motion — el equivalente exacto del template Viz Pilot / .mogrt con propiedades expuestas. El LLM director queda degradado al rol del periodista en la redacción: elige template del catálogo y llena campos; emitir un easing o una duración debe ser IMPOSIBLE por schema (JSON schema del director sin campos de motion), no solo prohibido por prompt. (3) SPEC POR MASTER (formato IBM): por cada elemento — transformación con valores exactos, token de duración, token de curva, delay, hold, y un choreography chart (Gantt en frames) + lista de técnicas prohibidas estilo constitución de Slack (p.ej. 'nunca animar carácter por carácter', 'nada entra sin el destello de transición'). (4) PROCESO DE LOCK: styleframes → motion test del master con datos dummy reales → aprobación de Manuel = picture-lock → solo entonces parametrizar; cambios post-lock re-abren el MASTER (una vez), jamás se ajusta motion en un video individual. (5) DE DÓNDE SALE LA CALIDAD: el master se puede iterar a mano las veces que haga falta (incluso adaptando referencia pro frame a frame) porque solo ocurre UNA vez por familia — el costo de dirección humana se amortiza sobre cientos de videos; esto es exactamente cómo CNBC/BBC producen motion de barra altísima con datos nuevos cada día sin un diseñador por pieza. Remotion es el renderer correcto para esto: su modelo useCurrentFrame() garantiza el determinismo frame-exacto que el patrón requiere. Lottie queda como opción táctica (@remotion/lottie) si algún master se autora en AE y se quiere reproducir EXACTO, pero el camino principal es tokens JSON + componentes-master con props de solo-datos.",
  "sources": [
    "https://m3.material.io/styles/motion/easing-and-duration/tokens-specs",
    "https://m1.material.io/motion/duration-easing.html",
    "https://carbondesignsystem.com/elements/motion/overview/",
    "https://www.npmjs.com/package/@carbon/motion",
    "https://medium.com/design-ibm/working-together-ux-motion-specs-for-design-and-development-249b12d60909",
    "https://base.uber.com/6d2425e9f/p/116184-motion",
    "https://tsmotion.design/slack-motion-design-guidelines",
    "https://runner.studio/blog/this-is-the-workflow-every-motion-design-studio-uses/",
    "https://www.designsystems.com/5-steps-for-including-motion-design-in-your-system/",
    "https://docs.vizrt.com/viz-artist-guide/5.4/Transition_Logic.html",
    "https://www.vizrt.com/products/viz-pilot-edge/",
    "https://www.vizrt.com/products/viz-trio/",
    "https://tkt1957.com/cnbc-vizrt-success-story/",
    "https://www.tvbeurope.com/live-production/we-wanted-a-studio-that-was-physical-not-virtual-behind-the-design-and-technology-of-bbc-news-studio-b",
    "https://www.newscaststudio.com/2019/07/17/bbc-news-rebrand-broadcast-design-2019/",
    "https://helpx.adobe.com/after-effects/using/creating-motion-graphics-templates.html",
    "https://www.designtokens.org/tr/drafts/format/",
    "https://www.w3.org/community/design-tokens/2025/10/28/design-tokens-specification-reaches-first-stable-version/",
    "https://lottiefiles.com/tutorials/how-to-hand-off-your-lottie-animation-to-developers-04oq0VrzTwo",
    "https://rive.app/blog/how-state-machines-work-in-rive",
    "https://www.remotion.dev/docs/flickering",
    "https://www.remotion.dev/docs/use-current-frame",
    "https://chyron.com/data-driven-graphics-workflows/"
  ]
}