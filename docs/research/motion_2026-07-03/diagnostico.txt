{
  "summary": "El \"trabado\" de la línea NO es un problema de easing sino de TÉCNICA de revelado: revelar por vértices (data truncada punto a punto) hace que la velocidad del trazo dependa de la longitud de cada segmento (segmentos desiguales = acelerones/frenazos) y, si la línea es suavizada (spline), la cabeza \"serpentea\" porque los puntos de control del último tramo se recalculan cada vez que entra un punto nuevo. El estándar profesional (CSS-Tricks, Jake Archibald, GSAP DrawSVG, todos los tutoriales de d3) es el opuesto: generar el path COMPLETO una sola vez y revelarlo por LONGITUD de trazo con stroke-dasharray/stroke-dashoffset — la geometría nunca cambia, solo cuánto se ve, y la velocidad es uniforme en el espacio de longitud del trazo. Remotion ya trae esto resuelto y determinista: @remotion/paths evolvePath(progress, path) devuelve exactamente strokeDasharray/strokeDashoffset por frame, más getLength()/getPointAtLength() para poner un punto tracer en la cabeza. No conviene raspar el path de ECharts (SSR renderToSVGString emite SVG pero sin IDs semánticos estables por serie); lo limpio es generar el path con d3-shape (d3.line().curve(d3.curveMonotoneX), sin overshoot, funciones puras = frame-exactas) usando las mismas escalas, y dejar ECharts fuera del motion de la línea. Para racing bars, la referencia canónica (Bostock, \"Bar Chart Race Explained\") usa n=12 barras, k=10 keyframes interpolados linealmente entre cada par de snapshots y 250 ms por keyframe con d3.easeLinear + textTween(interpolateNumber) → ~2.5 s por periodo de datos; las nuestras son ilegibles porque van muy por debajo de eso. Para count-ups, el default de facto de la industria (countUp.js) es 2 s con easeOutExpo (desacelera fuerte al final), y el plugin Odometer añade lastDigitDelay para que el último dígito asiente al final — exactamente la sensación de odómetro de la barra Terminal/igloo.",
  "key_points": [
    "DIAGNÓSTICO línea 'trabada' (1): revelar por vértices = la cabeza avanza un SEGMENTO de datos por unidad de tiempo, no una LONGITUD de trazo constante; segmentos de longitud dispar (datos densos/planos vs saltos) producen velocidad no uniforme. Es el mismo fenómeno documentado por GSAP para motion paths: 'plotting the progression... can make it appear to speed up or slow down based on... the length of each successive segment' (https://gsap.com/docs/v3/Plugins/MotionPathPlugin/).",
    "DIAGNÓSTICO línea 'trabada' (2): con curvas suavizadas, los puntos de control de cada tramo dependen de los puntos VECINOS (así definen sus splines d3/ECharts: https://d3js.org/d3-shape/curve); al truncar la data y añadir la cabeza interpolada, el último tramo se re-ajusta cada frame → la cabeza serpentea. Con dashoffset la geometría se congela una vez y nunca se recalcula.",
    "FIX #1 (el estándar pro, multi-fuente): path COMPLETO + stroke-dasharray=longitud total, stroke-dashoffset animado longitud→0. Es la técnica canónica de line-drawing en SVG: CSS-Tricks (https://css-tricks.com/svg-line-animation-works/), Jake Archibald (https://jakearchibald.com/2013/animated-line-drawing-svg/), y es EXACTAMENTE cómo funciona GSAP DrawSVGPlugin por dentro ('controls stroke-dashoffset and stroke-dasharray', https://gsap.com/docs/v3/Plugins/DrawSVGPlugin/). En d3 el patrón idéntico: getTotalLength() → transition de dashoffset a 0 (https://jaketrent.com/post/animating-d3-line/, https://www.visualcinnamon.com/2016/01/animating-dashed-line-d3/).",
    "FIX #1 en NUESTRO stack, verificado: @remotion/paths evolvePath(progress, path) devuelve {strokeDasharray, strokeDashoffset} listos para el <path> — ej. evolvePath(0.5, 'M 0 0 L 100 0') → {strokeDasharray:'100 100', strokeDashoffset:50} (https://www.remotion.dev/docs/paths/evolve-path). Por frame: progress = interpolate(frame,...,{easing}) → determinista y frame-exacto. getLength() (https://www.remotion.dev/docs/paths/get-length) y getPointAtLength() (https://www.remotion.dev/docs/paths/get-point-at-length) verificados → punto tracer esmeralda en la cabeza con getPointAtLength(getLength(d)*progress): viaja SIEMPRE suave porque se muestrea por longitud, no por vértice.",
    "FIX #2 (fuente del path): NO extraer el path de ECharts. Su SSR renderToSVGString sí emite SVG con animaciones CSS embebidas (https://echarts.apache.org/handbook/en/how-to/cross-platform/server/) pero los paths de serie no tienen ID semántico estable → parseo frágil. Generar el path directo con d3-shape: d3.line().x(...).y(...).curve(d3.curveMonotoneX) — curveMonotoneX preserva monotonicidad y NO hace overshoot (crítico en datos financieros: no inventa máximos/mínimos falsos) (https://d3js.org/d3-shape/curve, https://github.com/d3/d3/blob/main/docs/d3-shape/curve.md). d3-shape/d3-scale son funciones puras sin DOM ni loop interno → 100% deterministas en render server-side.",
    "FIX #2b (alternativa válida): revelado por clip-rect que avanza en x — es lo que hace el propio ECharts en su animación de entrada de línea (símbolos primero, línea conectando izquierda→derecha, con clip por sistema de coordenadas) (https://github.com/apache/incubator-echarts/issues/12523, https://echarts.apache.org/handbook/en/how-to/animation/transition/). Velocidad horizontal constante, cero serpenteo; la diferencia con dashoffset es velocidad-constante-en-x vs velocidad-constante-de-trazo (la firma 'se dibuja' pro es dashoffset). Útil para revelar el ÁREA bajo la línea con el mismo progress.",
    "FIX #3 (racing bars — números de la referencia canónica, verificados en el código de Bostock 'Bar Chart Race, Explained'): duration=250 ms por keyframe, k=10 keyframes interpolados LINEALMENTE (a(1-t)+bt) entre cada par de snapshots de datos, n=12 barras, d3.easeLinear en cada tramo (lineal para que la concatenación de keyframes no pulse), labels con textTween/d3.interpolateNumber (nunca saltan) (código: https://raw.githubusercontent.com/smithWEBtek/bar-chart-race-v2/master/f55121d560fe4f44@3007.js, notebook: https://observablehq.com/@d3/bar-chart-race-explained). Neto: ~2.5 s por periodo de datos. Nuestra regla: ≥2-2.5 s/periodo a 30fps (≥75 frames), valores interpolados linealmente entre snapshots, swaps de ranking animados en y con su propia duración (Flourish lo expone como setting separado 'bar swap duration' — el setting existe (https://rdrr.io/cran/flourishcharts/man/set_bar_chart_race_details.html) pero su default numérico NO está publicado: no verificable).",
    "FIX #4 (count-up/odómetro, verificado): countUp.js — default duration 2 s, default easing easeOutExpo (https://github.com/inorganik/countUp.js/blob/master/README.md); su plugin Odometer usa duraciones 2.3-3.0 s y lastDigitDelay para que el último dígito asiente al final (mismo README). Traducción a Remotion: progress con Easing.out(Easing.exp) (o bezier equivalente) sobre 60-75 frames; para el odómetro de dígitos que ruedan, cada columna de dígito recibe el mismo progress global pero el dígito de menor orden es el último en asentarse. Desacelerar al final = obligatorio; nuestro count lineal o corto (<1 s) se lee 'barato'.",
    "FIX #5 (bonus normalización): el atributo SVG pathLength permite normalizar cualquier path a longitud 100 (o 1) y animar dashoffset 100→0 sin medir (https://www.stefanjudis.com/today-i-learned/pathlength-makes-makes-svg-path-animations-easier-to-manage/) — útil si algún día el path no pasa por @remotion/paths. Nota histórica: por bugs viejos de dashoffset negativo en algunos motores, hay quien recomienda animar dasharray en vez de dashoffset (https://oreillymedia.github.io/Using_SVG/extras/ch13-drawing.html); en Chrome headless de Remotion no aplica, y evolvePath ya maneja los valores.",
    "NO VERIFICADO / honestidad: (a) defaults numéricos exactos de Flourish (animation_duration por periodo, bar swap duration) — el helpcenter devolvió 403 y la doc de la API no publica los defaults; solo queda verificado que ambos settings existen y son independientes. (b) No encontré un artículo que diga literalmente 'revelar por vértices serpentea': ese punto es inferencia técnica sólida a partir de la definición de splines (control points dependen de vecinos, doc oficial d3-curve) + el principio de velocidad-por-segmento documentado por GSAP MotionPath."
  ],
  "verdict_for_us": "APLICABLE 100% y con piezas ya en casa. El fix de la línea es quirúrgico y determinista: (1) generar el path UNA vez con d3.line().curve(d3.curveMonotoneX) sobre nuestras escalas (funciones puras, server-side, frame-exactas); (2) por frame, evolvePath(easedProgress, path) de @remotion/paths → dasharray/dashoffset exactos (cero estado, cero DOM measurement); (3) tracer esmeralda en la cabeza con getPointAtLength(getLength*progress) — el destello gris→esmeralda→hueso de la firma Terminal puede vivir en ese tracer. ECharts queda relegado: para la línea hero NO usar su data-truncada ni raspar su SVG; o se usa solo como layout de ejes/grid estático, o se reemplaza por d3-scale+d3-shape (recomendado: menos caja negra). Racing bars: adoptar los números de Bostock como master congelado — ≥75 frames (2.5 s) por periodo de datos, k=10 interpolación lineal entre snapshots, ≤12 barras, labels con interpolateNumber; a 30fps eso es parametrizable por dato sin re-aprobar motion. Count-up: 60-75 frames con Easing.out(Easing.exp), último dígito asienta al final — coincide con la estética de odómetro lento/contenido de la barra igloo. Todo es código puro sin aleatoriedad ni measurement en runtime → cumple el requisito de masters congelados que el LLM solo parametriza con datos.",
  "sources": [
    "https://css-tricks.com/svg-line-animation-works/",
    "https://jakearchibald.com/2013/animated-line-drawing-svg/",
    "https://gsap.com/docs/v3/Plugins/DrawSVGPlugin/",
    "https://gsap.com/docs/v3/Plugins/MotionPathPlugin/",
    "https://www.remotion.dev/docs/paths/evolve-path",
    "https://www.remotion.dev/docs/paths/get-length",
    "https://www.remotion.dev/docs/paths/get-point-at-length",
    "https://www.remotion.dev/docs/paths/",
    "https://d3js.org/d3-shape/curve",
    "https://github.com/d3/d3/blob/main/docs/d3-shape/curve.md",
    "https://jaketrent.com/post/animating-d3-line/",
    "https://www.visualcinnamon.com/2016/01/animating-dashed-line-d3/",
    "https://echarts.apache.org/handbook/en/how-to/cross-platform/server/",
    "https://echarts.apache.org/handbook/en/how-to/animation/transition/",
    "https://github.com/apache/incubator-echarts/issues/12523",
    "https://observablehq.com/@d3/bar-chart-race-explained",
    "https://raw.githubusercontent.com/smithWEBtek/bar-chart-race-v2/master/f55121d560fe4f44@3007.js",
    "https://github.com/inorganik/countUp.js/blob/master/README.md",
    "https://rdrr.io/cran/flourishcharts/man/set_bar_chart_race_details.html",
    "https://www.stefanjudis.com/today-i-learned/pathlength-makes-makes-svg-path-animations-easier-to-manage/",
    "https://oreillymedia.github.io/Using_SVG/extras/ch13-drawing.html"
  ]
}