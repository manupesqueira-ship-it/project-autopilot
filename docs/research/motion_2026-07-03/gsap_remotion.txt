{
  "summary": "GSAP dentro de Remotion es técnicamente viable y determinista con un patrón comunitario verificado: crear el timeline PAUSADO y hacer tl.seek(frame/fps) en cada frame que Remotion pide. GSAP es una librería basada en TIEMPO (cada tween es función pura del tiempo), así que el seek es frame-exacto por diseño; el problema famoso (\"GSAP corre a velocidad de reloj de pared mientras Remotion captura frames\") solo ocurre si dejas el timeline reproduciéndose con su ticker interno — pausado+seek lo elimina, y es exactamente la técnica que HyperFrames (HeyGen) usa en producción para renderizar HTML+GSAP a video. Post-adquisición Webflow, GSAP 3.13 (abril 2025) liberó TODO gratis incluso comercial: SplitText, MorphSVG, CustomEase, DrawSVG, etc.; la única restricción de licencia es no construir un builder visual de animaciones que compita con Webflow (código generado por IA está explícitamente permitido). El render de Remotion ocurre en Chrome headless real (hay DOM), así que SplitText funciona; el gotcha crítico es cargar fuentes ANTES de splitear (delayRender/loadFont waitUntilDone) porque el split por líneas depende de las métricas de la fuente. El patrón Terminal (palabra por palabra gris→esmeralda→hueso) es reproducible 1:1: SplitText por palabras + fromTo de color con stagger, seekeado por frame. No hay soporte oficial de Remotion (su doc solo enlaza un artículo archivado) y los ejemplos públicos de ALTA calidad dentro de Remotion son escasos — el patrón está probado, pero la calidad seguirá viniendo de los timelines master que se aprueben, no de GSAP en sí. Lo que GSAP compra es el LENGUAJE COMPARTIDO: los mismos timelines/easings/staggers de la web (menos lo scroll-driven de Lenis, que hay que re-expresar en tiempo) se ejecutan literalmente en el video.",
  "key_points": [
    "PATRÓN DETERMINISTA VERIFICADO (código real): hook useGsapTimeline — gsap.context() + timeline pausado en un useEffect de montaje, y segundo useEffect con timelineRef.current.seek(frame/fps) usando useCurrentFrame()+useVideoConfig(). Código completo verificado en https://github.com/hylarucoder/remotion-gsap-demo (src/Clip.tsx, mayo 2025), adaptado del CodeSandbox de Vladimir Topolev https://codesandbox.io/s/remotion-gsap-mgx16h y su artículo https://medium.com/@i-topolev-vladimir/how-to-integrate-greensock-with-remotion-e4eee6f5a41f (paywall; Remotion enlaza una copia archivada en su doc de terceros).",
    "POR QUÉ ES DETERMINISTA: GSAP modela cada tween como función pura del tiempo (timelines 'paused-and-seekable by design'); pausado, el ticker interno (performance.now) queda fuera del juego. La advertencia de que GSAP 'corre a wall-clock y Remotion captura frames vacíos' aplica SOLO al uso ingenuo sin seek — documentado por HyperFrames/HeyGen en https://hyperframes.heygen.com/guides/hyperframes-vs-remotion y https://www.heygen.com/research/html-to-video, que en producción usan exactamente pausa+seek(frame/fps) para render determinista de GSAP.",
    "REMOTION NO LO SOPORTA OFICIALMENTE: https://www.remotion.dev/docs/third-party solo dice que toda animación debe derivar de useCurrentFrame() y para GreenSock enlaza un artículo archivado; no hay paquete oficial. El hook es ~25 líneas y queda como código nuestro a mantener.",
    "GOTCHA #1 (concurrencia): Remotion renderiza en VARIAS pestañas paralelas que no comparten estado (https://www.remotion.dev/docs/flickering). El patrón seek sobrevive SOLO si la fábrica del timeline es PURA: prohibido gsap.utils.random / 'random(...)' en strings / Date.now dentro de los masters (cada pestaña crearía valores distintos → flicker entre chunks). Aleatoriedad solo con random() seedeado de Remotion pasado como parámetro.",
    "GOTCHA #2 (timing del efecto): el seek vive en useEffect, no en el render síncrono que Remotion recomienda; riesgo teórico de screenshot antes del flush (desfase de 1 frame). Mitigación: useLayoutEffect para el seek + QC en píxeles (frames reales, no metadata) — consistente con la doctrina R1 del proyecto. Los demos públicos renderizan correctamente con useEffect.",
    "PLUGINS GRATIS CONFIRMADO: GSAP 3.13 (abril/mayo 2025, post-adquisición Webflow oct-2024) liberó el 100% de la librería y TODOS los bonus plugins (SplitText, MorphSVG, DrawSVG, CustomEase, ScrollSmoother, ScrambleText...) incluso para uso comercial: https://gsap.com/blog/3-13/ + https://gsap.com/pricing/ + https://css-tricks.com/gsap-is-now-completely-free-even-for-commercial-use/. Licencia estándar (https://gsap.com/community/standard-license/): única prohibición = construir un builder visual de animaciones competidor de Webflow; 'AI-generated code using GSAP is permitted' explícito → nuestro pipeline automatizado es legal.",
    "SPLITTEXT EN RENDER HEADLESS: el render de Remotion ES un Chrome headless con DOM real, así que SplitText opera normal (no es Node sin DOM). Gotcha duro: splitear ANTES de que carguen las fuentes rompe el layout ('If you split before your web fonts are ready, the layout may shift or misalign' — https://gsap.com/docs/v3/Plugins/SplitText/). Solución en Remotion: cargar fuentes con @remotion/fonts loadFont()/waitUntilDone o delayRender+FontFace ANTES de montar el texto (https://www.remotion.dev/docs/fonts); con viewport fijo 1080x1920 los cortes de línea quedan estables. SplitText 3.13 fue reescrito (50% más chico, autoSplit + onSplit para re-split tras carga de fuente): https://webflow.com/blog/gsap-splittext-rewrite.",
    "PATRÓN TERMINAL 1:1: el destello palabra-por-palabra gris→esmeralda→hueso es un timeline trivial: SplitText type:'words' + tl.fromTo(words, {color:'#8A8A8A'}, {color:'#2ECB4F', stagger:0.06}).to(words, {color:'#E9E7E0', stagger:0.06}, '<0.1') — 100% seekeable por frame. Requisitos: fuentes cargadas pre-split y fábrica pura. No encontré impedimento técnico; lo que NO se transfiere de la web es lo scroll-driven (Lenis/ScrollTrigger): esos gestos hay que re-expresarlos como timelines de tiempo.",
    "ECOSISTEMA/EJEMPLOS: existen pero pocos y de nivel demo — hylarucoder/remotion-gsap-demo (efecto dados de CodePen creativeocean, video renderizado en README), CodeSandbox de Topolev, skill de agente 'gsap-animation' para Remotion (notedit, https://agentskills.so/skills/notedit-happy-skills-gsap-animation), vueseq (renderer determinista Vue+GSAP inspirado en Remotion, https://github.com/bennyzen/vueseq), llm-video-maker ('Deterministic HTML/GSAP render'). NO encontré un caso público de reels de clase mundial hechos con GSAP-en-Remotion: la validación fuerte del patrón en producción es HyperFrames (HeyGen), que eligió GSAP precisamente por ser seekeable.",
    "IMPLICACIÓN CLAVE PARA MASTERS: como el timeline es una función (factory) sobre un scope de elementos, el master aprobado en la web puede empaquetarse como factory parametrizable (textos/datos/duraciones) y ejecutarse idéntico en web (play con ticker) y en video (seek por frame) — un solo lenguaje de movimiento real, con CustomEase para clavar las curvas exactas de Terminal/igloo."
  ],
  "verdict_for_us": "SÍ: GSAP-en-Remotion PUEDE ser la fuente del motion de los reels, y es la opción con mejor encaje estratégico porque unifica literalmente el lenguaje web/video (mismos timelines, mismos easings CustomEase, mismo SplitText del patrón Terminal). Es determinista y frame-exacto (timeline pausado + seek(frame/fps); GSAP es función pura del tiempo), 100% gratis incluso comercial post-3.13, y el destello gris→esmeralda→hueso palabra por palabra es reproducible 1:1. CONDICIONES DURAS: (1) fábricas de timeline puras — cero random/Date.now, aleatoriedad solo seedeada por Remotion, o el render multi-pestaña flickerea; (2) fuentes cargadas con loadFont/delayRender ANTES de montar+splitear texto; (3) seek en useLayoutEffect + gate de QC en píxeles por el riesgo de desfase de 1 frame (el hook es comunitario, no oficial de Remotion); (4) lo scroll-driven de la web (Lenis/ScrollTrigger) NO se transfiere — se re-expresa como timelines de tiempo. RIESGO HONESTO: GSAP resuelve el DETERMINISMO y el VOCABULARIO (easings/staggers/SplitText), no la dirección: no hay precedente público de reels top hechos así, de modo que la barra la siguen poniendo los masters que Manuel apruebe — GSAP solo garantiza que ese master aprobado se reproduzca idéntico en cada render. Encaja además con la doctrina del proyecto: masters aprobados a mano → parametrizar solo datos.",
  "sources": [
    "https://github.com/hylarucoder/remotion-gsap-demo",
    "https://codesandbox.io/s/remotion-gsap-mgx16h",
    "https://medium.com/@i-topolev-vladimir/how-to-integrate-greensock-with-remotion-e4eee6f5a41f",
    "https://www.remotion.dev/docs/third-party",
    "https://www.remotion.dev/docs/flickering",
    "https://www.remotion.dev/docs/fonts",
    "https://www.remotion.dev/docs/use-current-frame",
    "https://hyperframes.heygen.com/guides/hyperframes-vs-remotion",
    "https://www.heygen.com/research/html-to-video",
    "https://gsap.com/blog/3-13/",
    "https://gsap.com/pricing/",
    "https://gsap.com/community/standard-license/",
    "https://gsap.com/docs/v3/Plugins/SplitText/",
    "https://webflow.com/blog/gsap-splittext-rewrite",
    "https://css-tricks.com/gsap-is-now-completely-free-even-for-commercial-use/",
    "https://tympanus.net/codrops/2025/05/14/from-splittext-to-morphsvg-5-creative-demos-using-free-gsap-plugins/",
    "https://agentskills.so/skills/notedit-happy-skills-gsap-animation",
    "https://github.com/bennyzen/vueseq",
    "https://github.com/GoldLegendW80/llm-video-maker"
  ]
}