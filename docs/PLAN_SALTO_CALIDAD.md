# Plan — Salto de calidad visual (Dinero LATAM)

> ⚠️ **SUPERSEDED (2026-06-19) → la verdad viva es [`PLAN_SALTO_CALIDAD_v2.md`](PLAN_SALTO_CALIDAD_v2.md).**
> Este v1 es el LOG de la fase de build (Track A–D, técnicamente completa). Se conserva como
> historial; NO es el plan vigente. Nota de API: las menciones a `ledger.SIGNATURE_EXCLUDE`
> (lista-NEGRA) reflejan el diseño de ESE momento — el freeze (Opción A) lo reemplazó por la
> lista-BLANCA `ledger.SIGNATURE_WOW`. La afirmación de fondo sigue válida (los hooks de
> set-piece los rota R11, NO son parte de la firma), solo cambió el mecanismo.

> **Objetivo:** subir DRÁSTICAMENTE la calidad visual (superar a 0x100x por ingenio,
> no por volumen). Todo $0 salvo gastos puntuales marcados **[👁️ avisar costo]**.
>
> **Regla de ejecución (Manuel, 2026-06-19):** construir TODOS los pasos primero
> (código / scripts / setup / docs). Los renders son lo más lento → se baten en
> **UN solo lote al final**, una vez que esté absolutamente todo hecho.

---

## Track A — Motor visual (lo hereda CADA beat)
- [x] **A1. Bug de timing** (Style Bible §7): ✅ VERIFICADO RESUELTO en `build916.py` (duración=`LEAD+dur(VO)+TAIL` por beat; cues atados a `words.json`; dip en hueco audible real; `filter_delivery.py` bloquea). Silencio ~1.0s = respiro intencional (§5.6), no bug. §7 actualizada.
- [x] **A2. Easing con overshoot** — ✅ `src/anim.ts` (curvas LOCKED §5.4: `SPRING.entrance/soft/settle`, `riseIn`/`popIn`/`riseScaleIn`, `stagger`). Cableado de referencia en BigNumberCounter (`rise`→`riseIn`) y StatCallout (`enterProgress`). Bundle OK (sin render). Resto de beats migran al tocarse / en R1.
- [x] **A3. Template Blender premium** — ✅ `infra/blender/studio_premium.py` (librería compartida que HEREDA cada beat 3D): OptiX GPU + salida PNG RGBA transparente + **motion blur** + filmic AgX + **HDRI** (archivo CC0 si existe, si no HDRI procedural de estudio $0) + **PBR** (metal/plástico/emisivo) + **tipografía 3D extruida con bisel** + cámara con **DoF** (TRACK_TO) + rig 3-point. Demo de validación `template_demo.py` (número hero "$209M" extruido, ejercita las 5 piezas). `py_compile` OK ambos; **render diferido a R1**. *Nota: coin_hero.py sigue intacto; migra al template cuando se toque.*
- [x] **A4. Gráficas 3D con volumen real** — ✅ `infra/blender/chart3d.py` (HEREDA `studio_premium`): barras cubo-3D biseladas (no el slab 2.5D de BarsValue.tsx), altura ∝ dato, materiales PBR de la paleta SEMÁNTICA (destacada=emisiva, resto=metal), **crecen escalonadas en el render** (mismo gesto que el grow de Remotion). Etiquetas valor+categoría **horneadas como texto 3D extruido** (Blender dibuja glifos EXACTOS; `--no-labels` las apaga y el dato lo pone Remotion). Sistema **propio $0** (no el addon BlenderDataVis → cero dependencia que instalar/mantener; mismo look que el resto de beats 3D). Consume la forma de `BarsValue.tsx` vía `--data c.json`; demo = tasas reales (capacidad NUEVA, no el CETES quemado). `encode_host.py` **generalizado** (`--dir/--prefix/--out/--fps/--no-upload`; defaults de la moneda intactos) → encodea las secuencias `demo_`/`bar_` local sin catbox. `py_compile` OK; **render diferido a R1**.
- [x] **A5. Post — acabado fílmico** — ✅ `infra/assembler/post_finish.py`: bloom (altas luces aisladas en luma → blur → screen-blend) + grano temporal + viñeta, **filtros NATIVOS de FFmpeg** (no Natron). *Decisión:* misma lógica que A4 rechazó el addon → FFmpeg YA es el ensamblador, así que cero dependencia que instalar/mantener, se aplica UNA vez sobre el master (consistente en todos los beats y costuras xfade, no por-beat), $0. Natron queda como upgrade opcional de R1, no como dependencia. **Cableado** en `build916.py`: si el guion trae `"finish": {bloom/grain/vignette/...}` se inyecta entre el trim y el fade final (cero pase extra de re-encode); SIN `"finish"` el grafo es **idéntico** al de hoy → no toca lo ya aprobado. Standalone `python post_finish.py IN.mp4 OUT.mp4 [--bloom .18 --grain 9 --vignette .3]` para A/B al ojo de Manuel. `py_compile` OK + grafo verificado (labels balanceados, passthrough en off); **render/tuneo diferido a R1**.

## Track B — Creatividad per-video (NUNCA prompt en blanco; casting con barandales)
- [x] **B1. Catálogo set-pieces + director** (`infra/n8n/setpiece_catalog.json`). Servilleta `napkin_2` ✅ aprobada.
- [x] **B2. Más set-pieces:** ✅ 4 componentes Remotion **$0** construidos con la MISMA arquitectura de la servilleta (objeto=foto IA / datos=overlay controlado / movimiento=cámara $0) y **fallback PROCEDURAL render-testable sin foto** (cero gasto para R1): `NewspaperSetPiece` (motion `headline_set`: titular que se imprime palabra×palabra + cifra resaltada; fallback = nameplate serif + reglas + columnas), `PhoneSetPiece` (`notification_in`: notificación entra deslizando + monto pop-in; fallback = cuerpo + isla dinámica + barra de estado), `ChalkboardSetPiece` (`write_on` multilínea: título + pasos con gis; fallback = pizarra + marco de madera + polvo), `TicketSetPiece` (`print_out`: tira que se imprime arriba→abajo + total resaltado; fallback = papel térmico + perforados + corte zigzag). **Wiring COMPLETO y verificado** (sin render): registrados en `Root.tsx` (4 `Composition` + `withSeams`); 4 builders en `proposer._SETPIECE_HOOK_BUILDERS` (helper `_sp_slug`: sin foto concreta → `""` → fallback procedural, así el gasto gpt-image queda gateado); 4 tipos en `validator.HOOK_TYPES` (R8 los toma solo de Root.tsx) y en `ledger.SIGNATURE_EXCLUDE` (los rota R11, no la firma); catálogo con `comp` exacto (sin "(por construir)"), 3 motions nuevas y **estado seguido `planeado`** (el director NO castea hasta que Manuel genere+apruebe la foto y ponga `estado:"listo"` + path real → cero cambio de código). Verificación: `py_compile` OK, JSON OK, suite validador **31/31**, los 4 hooks producen guion válido (errors=[]), handoff director→proposer emite el `comp` exacto, fallback procedural OK, **bundle Remotion limpio** + `tsc` sin errores en los nuevos archivos. *Asset foto-real (cuando Manuel decida activar c/u)* **[👁️ gpt-image ~$0.17 USD/≈$3 MXN c/u]**.
- [x] **B3. Lane hero-shot:** ✅ `infra/blender/hero_shot.py` (HEREDA `studio_premium`): mete un modelo 3D libre (Poly Haven CC0 / NASA dominio público / propio) al template premium + **camera move $0** (`swing` lento + `camera_move` push-in, con motion blur y DoF que acompaña el foco). Auto-encuadre (`frame_objects`: bbox→escala→pivote), import multi-formato (glb/gltf/obj/fbx/stl, tolerante a renombres de operador 3.x/4.x/5.x), override de material PBR opcional (paleta SEMÁNTICA). Mismo contrato de salida que la moneda/charts (PNG RGBA→`encode_host.py`→WebM→OffthreadVideo); el dato **NUNCA** se hornea en 3D (va en overlay Remotion). **Gobernanza de licencia EN CÓDIGO** (regla B3, no en prosa): `resolve_asset()` solo renderiza un asset REGISTRADO en `infra/blender/hero_assets.json` con `comercial_ok=true` y `brand_safe=true` (registrar = verificar licencia; mismo espíritu que el catálogo cerrado de set-pieces; aborta si no cumple). **Fallback PROCEDURAL $0 render-testable**: sin `--asset` genera un lingote de oro biselado (objeto "dinero" icónico) → ejercita TODO el lane sin descargar nada, para validar luz/DoF/blur en R1. `img2vid` de paga (Higgsfield/Kling) queda SOLO para movimiento físico real, gateado y aparte. Catálogo `hero_shot` actualizado (comp/motion/nota + ejemplos brand-safe: se quitó "SpaceX"=marca); el director lo **sugiere** en `_director` (gate de Manuel), nunca auto-castea. Verificación: `py_compile` OK, `hero_assets.json` JSON OK (1 entrada live `procedural_ingot` + ejemplos CC0/NASA/CC-BY para registrar), catálogo JSON OK. Sigue `estado:"planeado"` = el lane y su fallback YA corren; espera el modelo real + el gate de Manuel. **Render diferido a R1.** *Modelo foto-real (cuando Manuel registre c/u)* **[👁️ $0 si CC0/dominio público; img2vid de paga gateado aparte]**.
- [x] **B4. Triage en proposer/director:** ✅ `proposer.propose()` consulta a `director.direct()` ($0, código, sin render). Lane `set_piece` → el hook del set-piece (servilleta `BeatNapkin`) REEMPLAZA el hook de defecto; lane `default` → kinetic/stat. Barandales: `BeatNapkin` agregado a `HOOK_TYPES` (validator, R4) y a `SIGNATURE_EXCLUDE` (ledger, lo rota R11 no la firma). Set-piece hooks viven en `_SETPIECE_HOOK_BUILDERS` (separado del pool aleatorio → la servilleta NUNCA sale en un tema que no la dispara). Fallback limpio: si R11 choca el napkin con el video anterior, cae a hook de defecto. `hero_shot` se SUGIERE en metadata `_director` (gate de Manuel), nunca se auto-castea. Verificado: formula→napkin, no-formula→default, fallback R11→default; suite validador 31/31.

## Track C — Orden y gobernanza ($0)
- [x] **C1. Alinear docs stale:** ✅ Style Bible §8 reescrita (método propio $0) + header v2.2 + §9 (muertos: Envato AE/Nexrender/n8n, Veo, externos). `ROADMAP.md` con banner SUPERSEDED → apunta a la verdad viva.
- [ ] **C2. Apagar billing de Veo 3.1** (Manuel, en Google Cloud — ya no se usa).
- [x] **C3. Mascota / firma de marca** (Style Bible §5.8) — ✅ `infra/remotion-render/src/studio/BrandSignature.tsx`: **firma de marca recurrente $0** = "bug" de esquina reconocible en ~0.5s, presente en CADA video, que **respeta las safe-areas del 9:16** (la UI de IG tapa abajo/derecha) y entra con el easing LOCKED (§5.4, `popIn`) sin competir con el contenido. MISMA arquitectura de set-piece: el OBJETO de marca = asset foto-real en `public/brand/<slug>.png` (slug por prop, un tiro); **sin asset → placeholder PROCEDURAL $0** (chip neutro geométrico "dinero sube" + wordmark) render-testable HOY, así el gasto gpt-image queda gateado. gpt-image PNG o un frame hero del modelo 3D caen en el MISMO slot PNG (igual que `HeroCoin`). Registrado como **OVERLAY, no beat** (composición `BrandSignature` SIN prefijo `Beat` → el validador R8 NO lo castea; confirmado `BrandSignature ∉ tipos`, 44 beats intactos) con `preview:true` (backdrop) para que Manuel juzgue tamaño/posición/sutileza en R1. **Decisión de identidad de marca = de Manuel** (el componente es el SLOT + el sistema, no inventa la mascota): §5.8 deja gateado (a) **definir la mascota** (primer asset 3D orgánico), (b) aprobar el look, (c) prender el wire-up "en cada video" (overlay único en el ensamblado — NO se toca el master aprobado sin su OK). Verificado: `tsc` sin errores en archivos nuevos, **bundle Remotion limpio**, suite validador **31/31**. **Render diferido a R1.** *Asset mascota (cuando Manuel la defina)* **[👁️ gpt-image ~$3 MXN, o $0 si frame de modelo 3D CC0]**.

## Track D — Voz (Manuel)
- [ ] **D1. Voz clone:** grabar `docs/voice-clone/recording-script.md` → ElevenLabs Instant Clone → probar contra la voz Asgard actual (5/10 → meta 10/10).

---

## 🎬 Lote final de renders (hasta el final, tras A–D)
- [ ] **R1.** Render de validación: A3/A4/A5 + set-pieces nuevos + hero-shot en un video real → tunear luz/DoF/bloom al ojo/oído de Manuel. *Aquí se reactivan los renders.*

---
*Origen: foco 2026-06-19. La verdad viva del catálogo de set-pieces es `setpiece_catalog.json`; la del estilo es `docs/standards/DINERO_IA_STYLE_BIBLE.md`.*
